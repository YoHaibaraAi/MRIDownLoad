{
  "name": "rwj-dicom-downloader",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest V3 extension for downloading an authorized study as DICOM files.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-static.mjs && npm test"
  },
  "devDependencies": {
    "jszip": "3.10.2"
  }
}
